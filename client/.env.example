# ============================================================
# MMMS Client — Environment Variables
# ============================================================
# Copy to .env and set the correct API URL.
# In development, the Vite proxy handles /api routing —
# VITE_API_URL is only needed for production builds.
# ============================================================

VITE_API_URL=https://your-api.onrender.com
