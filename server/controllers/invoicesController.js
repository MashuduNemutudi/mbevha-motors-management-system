/**
 * controllers/invoicesController.js
 * ─────────────────────────────────────────────────────────────
 * Handles all invoices business logic for incoming requests.
 *
 * Functions to implement (see phase plan):
 *   getAll      GET    /api/invoices
 *   getOne      GET    /api/invoices/:id
 *   create      POST   /api/invoices
 *   update      PUT    /api/invoices/:id
 *   remove      DELETE /api/invoices/:id
 *   markPaid    PATCH  /api/invoices/:id/pay
 *   generatePDF GET    /api/invoices/:id/pdf
 * ─────────────────────────────────────────────────────────────
 * TODO: Implement all functions listed above.
 */

const db = require('../config/db');

// TODO: implement invoices controller functions

module.exports = {};
